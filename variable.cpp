#include<iostream>
using namespace std;

int main()
{
    int a;
    //   where 'a' is a variable ,which is mapping to a memory address 
    cin>>a; 
    // 'cin' is a input stream used to take input from user;
    cout<<a;
    // cout used to display the output on the screen 
}